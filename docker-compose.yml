services:
  app:
    build: .
    container_name: front-denuncias
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000

    volumes:
      - uploads_data:/app/uploads
    restart: unless-stopped

volumes:
  uploads_data:
    driver: local
